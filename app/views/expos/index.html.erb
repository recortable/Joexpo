<h1>Listing expos</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Background color</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @expos.each do |expo| %>
  <tr>
    <td><%= expo.name %></td>
    <td><%= expo.background_color %></td>
    <td><%= link_to 'Show', expo %></td>
    <td><%= link_to 'Edit', edit_expo_path(expo) %></td>
    <td><%= link_to 'Destroy', expo, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Expo', new_expo_path %>
